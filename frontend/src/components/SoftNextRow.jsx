import SoftNextCard from './SoftNextCard';

/**
 * O‘yin tugashi — SoftNext (SoftNextCard ustiga).
 * Eski API saqlanadi.
 */
export default function SoftNextRow(props) {
  return <SoftNextCard {...props} />;
}

export { DEFAULT_PLAY } from './SoftNextCard';
